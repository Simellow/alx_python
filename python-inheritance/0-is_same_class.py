"""function that returns true if the object is exactly an instance of the specified class and false otherwise"""
def is_same_class(obj, a_class):
    if obj in a_class:
        return True
    else: 
        False